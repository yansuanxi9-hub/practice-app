# 推荐题库文件格式

## JSON

推荐优先使用 JSON。字段稳定、错误提示最清楚，也最适合后续批量维护。

```json
[
  {
    "question": "教育的本质是什么？",
    "options": {
      "A": "培养人的社会活动",
      "B": "单纯传授知识",
      "C": "考试训练",
      "D": "自然成长"
    },
    "answer": "A",
    "explanation": "教育是一种有目的地培养人的社会活动。",
    "type": "single",
    "source": "教育学原理"
  }
]
```

要求：

- `question`：题干，不能为空。
- `options`：必须包含 `A`、`B`、`C`、`D`。
- `answer`：必须是 `A`、`B`、`C`、`D` 之一。
- `type`：当前只支持 `single`。
- `source`：题库来源，会优先作为导入后的题库名称。

## CSV

第一行必须是固定表头：

```csv
question,A,B,C,D,answer,explanation,type,source
教育的本质是什么？,培养人的社会活动,单纯传授知识,考试训练,自然成长,A,教育是一种有目的地培养人的社会活动。,single,教育学原理
```

如果题干或解析里包含英文逗号，请用英文双引号包起来。

## TXT

每道题使用固定模板。多道题可以连续写，每道题从 `题目：` 开始。

```text
题目：教育的本质是什么？
A. 培养人的社会活动
B. 单纯传授知识
C. 考试训练
D. 自然成长
答案：A
解析：教育是一种有目的地培养人的社会活动。
来源：教育学原理
```

## DOCX

docx 只建议放纯文字题库，不要放图片题。App 会尝试从 `word/document.xml` 提取文字，再按 TXT 模板解析。

网页版本已经内置 `JSZip`，可以读取普通 Word 生成的压缩 `.docx`。文档内容请按 TXT 模板排版：

```text
题目：教育的本质是什么？
A. 培养人的社会活动
B. 单纯传授知识
C. 考试训练
D. 自然成长
答案：A
解析：教育是一种有目的地培养人的社会活动。
来源：教育学原理
```

限制：

- 当前不支持图片题。
- 仅支持 `.docx`，不支持旧版 `.doc`。
- SwiftUI 原生 App 版本仍未接入第三方 ZIP 库；如果要在原生 App 里完整支持 docx，建议用 Swift Package Manager 添加 `ZIPFoundation`。

## PDF

PDF 只支持可复制的文字题库，不支持扫描图片版 PDF。内容仍按 TXT 模板排版：

```text
题目：教育的本质是什么？
A. 培养人的社会活动
B. 单纯传授知识
C. 考试训练
D. 自然成长
答案：A
解析：教育是一种有目的地培养人的社会活动。
来源：教育学原理
```

限制：

- 当前不支持扫描版 PDF 和图片题。
- PDF 文字提取可能把换行或空格打乱，导入失败时建议先转成 TXT 后整理。
- 对于用选项字母下方短横线标记答案的 PDF，网页版本会尝试识别正确答案。
- 对于包含 `第一章`、`第二章` 等标题的长 PDF，网页版本会按章节拆分成多个题库。
- 网页版本使用本地 `PDF.js` 解析，不会上传文件。
